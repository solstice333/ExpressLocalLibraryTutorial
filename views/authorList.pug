extends layout

block content
   h1= title
   ul
      each author in authorList
         li 
            a(href=author.url) #{author.name} 
            | (#{author.date_of_birth_formatted || "Unknown"} - #{author.date_of_death_formatted || "Present"})
      else
         li There are no authors.
